<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.tale.prettybundle.sample"
    xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        >
        <activity
            android:name=".MenuActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".TestStringExtra1Activity"/>
        <activity android:name=".TestStringExtra2Activity"/>
        <activity android:name=".TestPrimaryTypeSetterActivity"/>
        <activity android:name=".TestPrimaryTypeDisplayActivity"/>
    </application>

</manifest>
